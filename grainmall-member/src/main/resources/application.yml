spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.9.3:3306/grainmall_ums?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: grainmall-member

mybatis-plus:
  mapper-locations: classpath:*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 8000